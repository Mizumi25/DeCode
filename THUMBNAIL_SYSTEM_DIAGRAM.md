# 🎨 Thumbnail System - Visual Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         YOUR THUMBNAIL SYSTEM                        │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────────┐                    ┌──────────────────────┐
│   Frame Thumbnails   │                    │  Project Thumbnails  │
│    (Individual)      │                    │  (Entire Canvas)     │
├──────────────────────┤                    ├──────────────────────┤
│ Backend - Playwright │                    │ Frontend - Canvas API│
│ PlaywrightThumb...   │                    │ VoidPageSnapshot...  │
│ PHP Service          │                    │ JavaScript Service   │
├──────────────────────┤                    ├──────────────────────┤
│ Used for:            │                    │ Used for:            │
│ • Frame previews     │                    │ • ProjectList grid   │
│ • Individual cards   │                    │ • Zoomed view        │
│ • Inside Void editor │                    │ • Project overview   │
├──────────────────────┤                    ├──────────────────────┤
│ Storage:             │                    │ Storage:             │
│ thumbnails/frames/   │                    │ thumbnails/projects/ │
└──────────────────────┘                    └──────────────────────┘
```

---

## Project Thumbnail Generation Flow (V2 - Framer Style)

```
┌─────────────────────────────────────────────────────────────────────┐
│                              VOID PAGE                               │
│                                                                      │
│  ┌────────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │   Frame 1      │  │   Frame 2   │  │   Frame 3   │             │
│  │  [Home Page]   │  │ [About Page]│  │[Contact Pg] │             │
│  │                │  │             │  │             │             │
│  │  [thumbnail]   │  │ [thumbnail] │  │ [thumbnail] │             │
│  └────────────────┘  └─────────────┘  └─────────────┘             │
│                                                                      │
│  ┌──────────────────────────────────────────────────┐              │
│  │         FloatingToolbox (Left Side)              │              │
│  │  [+] [Layers] [Files] [Code] [Team]             │              │
│  │  [📷 Camera] ← Click here to generate thumbnail  │              │
│  └──────────────────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
                    User clicks Camera button
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│              VoidPageSnapshotService.generateAndUpload()            │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      STEP 1: Data Collection                         │
├─────────────────────────────────────────────────────────────────────┤
│  • Find Void canvas: document.querySelector('[data-canvas="true"]') │
│  • Find all frames: canvas.querySelectorAll('[data-frame-uuid]')    │
│  • For each frame, collect:                                         │
│    - Position (x, y, width, height)                                 │
│    - Styles (backgroundColor, borderColor, boxShadow)               │
│    - Content (title, thumbnail image, iframe)                       │
│    - DOM reference                                                  │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    STEP 2: Viewport Calculation                      │
├─────────────────────────────────────────────────────────────────────┤
│  • Calculate bounds of all frames                                   │
│  • Add padding (50px)                                               │
│  • Calculate scale to fit in 1600x1000                              │
│  • Calculate centering offsets                                      │
│                                                                      │
│  Example:                                                           │
│    All frames span: 0,0 to 2400,1500                               │
│    With padding: -50,-50 to 2450,1550                              │
│    Scale to fit: 0.64x                                             │
│    Centered at: (200, 120)                                         │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    STEP 3: Canvas Creation                           │
├─────────────────────────────────────────────────────────────────────┤
│  const canvas = document.createElement('canvas');                   │
│  canvas.width = 1600;                                               │
│  canvas.height = 1000;                                              │
│  const ctx = canvas.getContext('2d');                               │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                   STEP 4: Background Rendering                       │
├─────────────────────────────────────────────────────────────────────┤
│  // Draw Void's signature gradient                                  │
│  const gradient = ctx.createLinearGradient(0, 0, 1600, 1000);      │
│  gradient.addColorStop(0, '#0f172a');    // slate-900              │
│  gradient.addColorStop(0.5, '#581c87');  // purple-900             │
│  gradient.addColorStop(1, '#0f172a');    // slate-900              │
│  ctx.fillStyle = gradient;                                          │
│  ctx.fillRect(0, 0, 1600, 1000);                                   │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                   STEP 5: Image Preloading                           │
├─────────────────────────────────────────────────────────────────────┤
│  // Preload all frame thumbnail images                              │
│  for each frame:                                                    │
│    const img = new Image();                                         │
│    img.src = frame.thumbnailImg.src;                                │
│    await img.onload;                                                │
│  // Now all images are ready to draw!                               │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                   STEP 6: Frame Rendering                            │
│                    (The Magic Happens Here!)                         │
├─────────────────────────────────────────────────────────────────────┤
│  FOR EACH FRAME:                                                    │
│                                                                      │
│  1. Calculate position on canvas                                    │
│     canvasX = offsetX + ((frameX - viewportX) * scale)             │
│     canvasY = offsetY + ((frameY - viewportY) * scale)             │
│     canvasW = frameWidth * scale                                    │
│     canvasH = frameHeight * scale                                   │
│                                                                      │
│  2. Draw frame container                                            │
│     • Shadow (if boxShadow exists)                                  │
│     • Background (backgroundColor)                                  │
│     • Border (borderColor, borderWidth)                             │
│     • Rounded corners (borderRadius)                                │
│                                                                      │
│  3. Draw frame header                                               │
│     • Header background (light gray)                                │
│     • Title text (frame.title)                                      │
│     • Separator line                                                │
│                                                                      │
│  4. Draw frame content (THE KEY PART!)                              │
│     IF thumbnail image exists:                                      │
│       ctx.drawImage(thumbnailImg, contentX, contentY, w, h)        │
│       ← This draws the ACTUAL frame preview!                        │
│     ELSE:                                                           │
│       Draw placeholder (gradient + mock content)                    │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                   STEP 7: Image Conversion                           │
├─────────────────────────────────────────────────────────────────────┤
│  const dataUrl = canvas.toDataURL('image/jpeg', 0.95);             │
│  const blob = await fetch(dataUrl).then(r => r.blob());            │
│                                                                      │
│  Result: High-quality JPEG image (~200-500KB)                       │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      STEP 8: Upload to Backend                       │
├─────────────────────────────────────────────────────────────────────┤
│  POST /api/projects/{uuid}/thumbnail/snapshot                       │
│  FormData:                                                          │
│    - snapshot: blob (JPEG file)                                     │
│    - method: 'framer_style_v2'                                      │
│                                                                      │
│  Backend (ProjectController::updateThumbnailFromSnapshot):          │
│    1. Validate image (max 5MB)                                      │
│    2. Delete old thumbnail (if exists)                              │
│    3. Store new: storage/app/public/thumbnails/projects/            │
│    4. Update project.thumbnail field                                │
│    5. Return thumbnail URL                                          │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                         STEP 9: Success!                             │
├─────────────────────────────────────────────────────────────────────┤
│  alert('✅ Project thumbnail generated successfully!');             │
│                                                                      │
│  Project thumbnail is now stored and can be viewed in ProjectList!  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## How Frames Are Rendered (Detailed)

```
┌────────────────────────────────────────────────────────────────┐
│                    Frame Rendering Detail                       │
│                   (renderSingleFrame method)                    │
└────────────────────────────────────────────────────────────────┘

Input:
  frame = {
    uuid: 'abc123',
    x: 100, y: 100,
    width: 400, height: 600,
    backgroundColor: '#ffffff',
    borderColor: '#e5e7eb',
    borderRadius: 8,
    boxShadow: '0 4px 6px rgba(0,0,0,0.1)',
    title: 'Home Page',
    thumbnailImg: <Image object>
  }
  
  canvasX = 300, canvasY = 200
  canvasWidth = 256, canvasHeight = 384

┌────────────────────────────────────────────────────────────────┐
│ LAYER 1: Shadow                                                 │
├────────────────────────────────────────────────────────────────┤
│  ctx.shadowColor = 'rgba(0, 0, 0, 0.15)';                      │
│  ctx.shadowBlur = 15;                                          │
│  ctx.shadowOffsetY = 4;                                        │
│  roundRect(canvasX, canvasY, canvasWidth, canvasHeight, 8);    │
│  ctx.fill();                                                   │
└────────────────────────────────────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────────────┐
│ LAYER 2: Background                                             │
├────────────────────────────────────────────────────────────────┤
│  ctx.fillStyle = '#ffffff';                                    │
│  roundRect(canvasX, canvasY, canvasWidth, canvasHeight, 8);    │
│  ctx.fill();                                                   │
└────────────────────────────────────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────────────┐
│ LAYER 3: Border                                                 │
├────────────────────────────────────────────────────────────────┤
│  ctx.strokeStyle = '#e5e7eb';                                  │
│  ctx.lineWidth = 1;                                            │
│  roundRect(canvasX, canvasY, canvasWidth, canvasHeight, 8);    │
│  ctx.stroke();                                                 │
└────────────────────────────────────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────────────┐
│ LAYER 4: Header Background                                      │
├────────────────────────────────────────────────────────────────┤
│  headerHeight = canvasHeight * 0.12;  // 46px                  │
│  ctx.fillStyle = 'rgba(0, 0, 0, 0.02)';                        │
│  ctx.fillRect(canvasX, canvasY, canvasWidth, headerHeight);    │
└────────────────────────────────────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────────────┐
│ LAYER 5: Header Title                                           │
├────────────────────────────────────────────────────────────────┤
│  ctx.fillStyle = '#1f2937';                                    │
│  ctx.font = '600 10px -apple-system, sans-serif';              │
│  ctx.fillText('Home Page', canvasX + padding, canvasY + 23);   │
└────────────────────────────────────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────────────┐
│ LAYER 6: Header Separator                                       │
├────────────────────────────────────────────────────────────────┤
│  ctx.strokeStyle = 'rgba(0, 0, 0, 0.05)';                      │
│  ctx.beginPath();                                              │
│  ctx.moveTo(canvasX, canvasY + headerHeight);                  │
│  ctx.lineTo(canvasX + canvasWidth, canvasY + headerHeight);    │
│  ctx.stroke();                                                 │
└────────────────────────────────────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────────────┐
│ LAYER 7: Frame Content (THE ACTUAL PREVIEW!)                    │
├────────────────────────────────────────────────────────────────┤
│  contentX = canvasX + padding;                                 │
│  contentY = canvasY + headerHeight + padding;                  │
│  contentW = canvasWidth - (padding * 2);                       │
│  contentH = canvasHeight - headerHeight - (padding * 3);       │
│                                                                 │
│  IF thumbnailImg exists:                                        │
│    // THIS IS THE KEY - Drawing the actual frame thumbnail!    │
│    ctx.drawImage(                                              │
│      thumbnailImg,     ← Preloaded image of frame content      │
│      contentX,         ← Position X                            │
│      contentY,         ← Position Y                            │
│      contentW,         ← Width (scaled)                        │
│      contentH          ← Height (scaled)                       │
│    );                                                          │
│    ← This renders the REAL frame preview, not a placeholder!   │
│                                                                 │
│  ELSE:                                                          │
│    // Draw placeholder gradient + mock content                 │
│    const gradient = ctx.createLinearGradient(...);             │
│    ctx.fillRect(contentX, contentY, contentW, contentH);       │
│                                                                 │
└────────────────────────────────────────────────────────────────┘

Result: A complete, high-fidelity frame preview!
```

---

## Comparison: V1 vs V2 Approach

### V1 (OLD) - DOM Cloning Approach:
```
┌─────────────────────────────────────────────┐
│ 1. Find Void canvas element                 │
│ 2. Clone the ENTIRE element                 │
│ 3. Remove unwanted elements from clone      │
│ 4. Mount clone in offscreen container       │
│ 5. Wait for render                          │
│ 6. Try to capture with Canvas API           │
│    ↓                                         │
│    ❌ PROBLEMS:                              │
│    • Cloning often fails                    │
│    • Frames don't copy correctly            │
│    • Styles don't apply                     │
│    • Thumbnails missing                     │
│    • Complex edge cases                     │
└─────────────────────────────────────────────┘
```

### V2 (NEW) - Direct Rendering Approach:
```
┌─────────────────────────────────────────────┐
│ 1. Find Void canvas element                 │
│ 2. Read data from each frame                │
│ 3. Collect styles, positions, content       │
│ 4. Create blank canvas                      │
│ 5. Manually render each element using:      │
│    • ctx.fillRect() for backgrounds         │
│    • ctx.strokeRect() for borders           │
│    • ctx.fillText() for titles              │
│    • ctx.drawImage() for thumbnails         │
│    ↓                                         │
│    ✅ RESULTS:                               │
│    • Pixel-perfect rendering                │
│    • Full control over output               │
│    • Actual thumbnails shown                │
│    • No cloning needed                      │
│    • Simple, maintainable                   │
└─────────────────────────────────────────────┘
```

---

## The Key Innovation: Actual Thumbnail Images

### What Makes This Framer-Style:

```
❌ OLD WAY (html2canvas approach):
   Take a screenshot of the DOM → Often inaccurate

❌ V1 WAY (clone and capture):
   Clone DOM → Try to capture → Often fails

✅ V2 WAY (Framer approach):
   For each frame:
     1. Get the frame's thumbnail image (already generated by Playwright)
     2. Draw it directly onto canvas at the right position
     3. Add proper frame chrome (border, shadow, title)
   
   Result: You see the ACTUAL frame content in the thumbnail,
           not a placeholder or failed clone!
```

### Visual Example:

```
Frame in Void:
┌─────────────────────────┐
│ Home Page           [×] │  ← Header with title
├─────────────────────────┤
│                         │
│   [Actual website       │  ← This is a thumbnail image
│    preview showing      │     generated by Playwright
│    the real HTML/CSS]   │
│                         │
└─────────────────────────┘

What V2 captures:
┌─────────────────────────┐
│ Home Page               │  ← Draws header with title
├─────────────────────────┤
│                         │
│   [SAME actual website  │  ← Draws the thumbnail image
│    preview - pixel-     │     using ctx.drawImage()
│    perfect copy!]       │
│                         │
└─────────────────────────┘

What V1 captured:
┌─────────────────────────┐
│ Home Page               │
├─────────────────────────┤
│                         │
│   [Empty gray box       │  ← Clone failed, no content
│    or broken elements]  │
│                         │
└─────────────────────────┘
```

---

## Summary Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                  YOUR FRAMER-STYLE SYSTEM                        │
└─────────────────────────────────────────────────────────────────┘

┌───────────┐     ┌──────────────┐     ┌────────────────┐
│ VoidPage  │────▶│ Camera Button│────▶│ Generate       │
│ (Editor)  │     │ (FloatingTool)│     │ Thumbnail      │
└───────────┘     └──────────────┘     └────────────────┘
                                              │
                                              ▼
                                  ┌───────────────────────┐
                                  │ VoidPageSnapshot      │
                                  │ Service V2            │
                                  ├───────────────────────┤
                                  │ 1. Collect data       │
                                  │ 2. Calculate viewport │
                                  │ 3. Create canvas      │
                                  │ 4. Draw background    │
                                  │ 5. Preload images     │
                                  │ 6. Render frames      │
                                  │ 7. Convert to JPEG    │
                                  │ 8. Upload             │
                                  └───────────────────────┘
                                              │
                                              ▼
                              ┌───────────────────────────────┐
                              │ Backend                       │
                              │ (ProjectController)           │
                              ├───────────────────────────────┤
                              │ • Validate image              │
                              │ • Store in storage/           │
                              │ • Update project.thumbnail    │
                              │ • Return URL                  │
                              └───────────────────────────────┘
                                              │
                                              ▼
                                  ┌───────────────────────┐
                                  │ ProjectList           │
                                  ├───────────────────────┤
                                  │ • Grid view shows     │
                                  │   thumbnail           │
                                  │ • Click to zoom       │
                                  │ • Full-screen preview │
                                  └───────────────────────┘
```

---

**Your system now works like Framer - capturing high-fidelity project previews by directly rendering each frame with its actual content!**
